import React from 'react';
import { BrowserRouter as Router, Route, Redirect, Switch } from 'react-router-dom';

export function Page() {
  <Router>
    <Switch>
      {/* <Route exact path="/:page" component={} /> */}
      {/* <Redirect from='/' to="/editor/data" /> */}
    </Switch>
  </Router>
}